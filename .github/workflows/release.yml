# name: Release

# # Builds multi-platform binaries and a Docker image when a version tag is pushed
# # (vX.Y.Z) and attaches the binaries to a (draft) GitHub Release.
# # Cut a release by pushing a version tag: `git tag vX.Y.Z && git push --tags`.

# on:
#   push:
#     tags:
#       - "v*.*.*"

# permissions:
#   contents: write
#   packages: write

# env:
#   CARGO_TERM_COLOR: always

# jobs:
#   build:
#     name: Build ${{ matrix.target }}
#     runs-on: ${{ matrix.os }}
#     strategy:
#       fail-fast: false
#       matrix:
#         include:
#           - { os: ubuntu-latest, target: x86_64-unknown-linux-gnu, ext: "" }
#           - { os: ubuntu-latest, target: aarch64-unknown-linux-gnu, ext: "" }
#           - { os: macos-latest, target: x86_64-apple-darwin, ext: "" }
#           - { os: macos-latest, target: aarch64-apple-darwin, ext: "" }
#           - { os: windows-latest, target: x86_64-pc-windows-msvc, ext: ".exe" }
#     steps:
#       - uses: actions/checkout@v4
#       - name: Install Rust
#         run: |
#           rustup show
#           rustup target add ${{ matrix.target }}
#       # aarch64 Linux is cross-compiled from an x86_64 runner: this installs the
#       # cross linker/gcc and sets CARGO_TARGET_*_LINKER + CC_*/CXX_*/AR_* so the C
#       # deps (aws-lc-rs, ring, zstd-sys) build for the target. Native targets skip it.
#       - name: Set up cross toolchain (Linux aarch64)
#         if: matrix.target == 'aarch64-unknown-linux-gnu'
#         uses: taiki-e/setup-cross-toolchain-action@v1
#         with:
#           target: aarch64-unknown-linux-gnu
#       - uses: Swatinem/rust-cache@v2
#       - name: Build server + cli (release)
#         run: cargo build --release --target ${{ matrix.target }} -p nusadb-server -p nusadb-cli
#       - name: Package
#         shell: bash
#         run: |
#           mkdir -p dist
#           BIN_DIR="target/${{ matrix.target }}/release"
#           NAME="nusadb-${{ github.ref_name }}-${{ matrix.target }}"
#           mkdir -p "$NAME"
#           cp "$BIN_DIR/nusadb-server${{ matrix.ext }}" "$NAME/" 2>/dev/null || true
#           cp "$BIN_DIR/nusa-cli${{ matrix.ext }}" "$NAME/" 2>/dev/null || \
#             cp "$BIN_DIR/nusadb-cli${{ matrix.ext }}" "$NAME/" 2>/dev/null || true
#           cp README.md LICENSE "$NAME/"
#           if [[ "${{ matrix.os }}" == "windows-latest" ]]; then
#             7z a "dist/$NAME.zip" "$NAME"
#           else
#             tar czf "dist/$NAME.tar.gz" "$NAME"
#           fi
#       - name: Upload artifacts
#         uses: actions/upload-artifact@v4
#         with:
#           name: nusadb-${{ matrix.target }}
#           path: dist/*

#   # Builds and publishes the multi-arch server image to Docker Hub and GHCR. The
#   # Dockerfile at the repo root is the same one used for local `docker build`
#   # (single source of truth). Requires the DOCKERHUB_USERNAME / DOCKERHUB_TOKEN
#   # repo secrets (the token needs Read & Write scope).
#   docker:
#     name: Docker image (linux/amd64, linux/arm64)
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       packages: write
#     steps:
#       - uses: actions/checkout@v4
#       - name: Set up QEMU
#         uses: docker/setup-qemu-action@v3
#       - name: Set up Buildx
#         uses: docker/setup-buildx-action@v3
#       - name: Log in to Docker Hub
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.DOCKERHUB_USERNAME }}
#           password: ${{ secrets.DOCKERHUB_TOKEN }}
#       - name: Log in to GHCR
#         uses: docker/login-action@v3
#         with:
#           registry: ghcr.io
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}
#       - name: Image metadata (tags + labels)
#         id: meta
#         uses: docker/metadata-action@v5
#         with:
#           # Publish under both registries; Docker Hub repo = <DOCKERHUB_USERNAME>/nusadb.
#           images: |
#             ${{ secrets.DOCKERHUB_USERNAME }}/nusadb
#             ghcr.io/${{ github.repository }}
#           tags: |
#             type=semver,pattern={{version}}
#             type=semver,pattern={{major}}.{{minor}}
#             type=raw,value=latest
#       - name: Build and push
#         uses: docker/build-push-action@v6
#         with:
#           context: .
#           platforms: linux/amd64,linux/arm64
#           push: true
#           tags: ${{ steps.meta.outputs.tags }}
#           labels: ${{ steps.meta.outputs.labels }}
#           cache-from: type=gha
#           cache-to: type=gha,mode=max

#   release:
#     name: Publish GitHub Release
#     needs: [build, docker]
#     runs-on: ubuntu-latest
#     permissions:
#       contents: write
#     steps:
#       - uses: actions/download-artifact@v4
#         with:
#           path: dist
#           merge-multiple: true
#       - name: Create release
#         uses: softprops/action-gh-release@v2
#         with:
#           files: dist/*
#           generate_release_notes: true
#           draft: true # review before publishing
